language: java

sudo: required

script:
 - ./gradlew check

before_install:
 - chmod +x gradlew
 - mysql -e "create database IF NOT EXISTS webapp;" -uroot

jdk:
 - oraclejdk8
 - openjdk8

services:
- mysql

notifications:
  email: false
